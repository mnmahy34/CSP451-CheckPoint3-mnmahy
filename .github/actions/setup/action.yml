name: "Setup Node & Install"
description: "Reusable setup step for Node.js"
runs:
  using: "composite"
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: '20'
    - run: npm install
      shell: bash
